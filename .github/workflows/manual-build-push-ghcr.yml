name: Manual deploy to ghcr

on:
  workflow_dispatch:

jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@add-tag-strat # v2024.09.02
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write