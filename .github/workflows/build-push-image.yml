name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@2c853f9399d5c83c1f7a429eb0bcb0619b701f23 # v2024.11.27
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write