name: Build & Push

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:

jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@85791061bfaf6de89238a64e2b7eb4d1d32f5f88 # v2025.04.18
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
