name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@89f1987d0331505106950f1f3466e0de0fac3d6e # v2024.10.29
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write