name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@af86a6f83760d7e6dbdfa5538ef99be45dda8747 # v2024.10.18
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write