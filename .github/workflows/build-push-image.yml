name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@dd9c6f29abb9ba3cabcfc2c04944c66edeee6bd8 # v2025.01.10
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write