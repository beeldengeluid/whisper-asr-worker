name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@fbcb98e53fc960f06029d3ef281461d73f2e37fd # v2024.11.22
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write