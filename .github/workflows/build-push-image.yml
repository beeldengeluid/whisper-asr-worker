name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@d0ec8775b756729899e7f695ea12aeff948e6859 # v2025.02.14
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write