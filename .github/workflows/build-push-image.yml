name: Build & Push

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:

jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@d4776cb08fd81c03e1dda867308d33bf958f4176 # v2025.11.12
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
