name: Deploy dane-whisper-asr-worker to ghcr

on:
  push:
    branches:
    -  main
  workflow_call:
  workflow_dispatch:
jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@0962147f9ddc8aa1d4a69d821b7f51ea06015984 # v2025.02.26
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write